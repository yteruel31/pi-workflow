# v0.1.0 validation evidence

This record distinguishes observed package evidence from model-dependent workflow behavior. It does not claim that prompt instructions were enforced at runtime.

## Observed before the final review-fix batch

- Pi version: `0.80.10`.
- Public installation from `git:github.com/yteruel31/pi-workflow` succeeded.
- The installed checkout and source repository both resolved to commit `9c3779b2108a83b93f38d6e76c0bf338b1eab85f`.
- The installed package contained exactly the four skill files for `yt-brainstorm`, `yt-plan`, `yt-work`, and `yt-review`.
- The pre-fix dependency-free contract suite passed all 21 tests.
- `npm pack --dry-run` passed and reported the intended package contents.

## Pending revalidation after the final fix commit

- Run the updated contract suite and record its final test count.
- Run the package dry-run against the committed tree.
- Push the final commit and publish the `v0.1.0` tag, then verify both remote refs.
- Update or reinstall the public Git package and verify that installed and source commits match.
- Exercise direct live invocation of all four skills.
- Exercise model-dependent worker behavior: sequential units, foreign-change isolation, failed-unit stopping, parent-owned commits, and missing-role fallback.
- Exercise model-dependent review behavior: one/two/three-reviewer routing, report-only compliance, observable local-state comparison, remote-evidence reporting, and missing-role fallback.

The live skill, worker, and reviewer scenarios above have not yet been executed and remain release validation gaps.
